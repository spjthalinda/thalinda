export type ESLintParseResult = import('eslint').Linter.ESLintParseResult;
export type Options = {
    filePath: string;
};
/**
 * @param {string} code
 * @param {Options} options
 * @returns {ESLintParseResult} parse result with AST
 */
export function parseForESLint(code: string, options: Options): ESLintParseResult;
export declare function parse(code: string, options: Options): import("eslint").AST.Program;
