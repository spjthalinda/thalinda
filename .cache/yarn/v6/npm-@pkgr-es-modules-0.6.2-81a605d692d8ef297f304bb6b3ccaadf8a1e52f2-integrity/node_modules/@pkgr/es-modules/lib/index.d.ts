export declare type ModuleDefinition = string | {
    target: string;
    exact?: boolean;
};
export declare type ModuleDefinitions = Record<string, ModuleDefinition>;
export declare const MODULE_DEFINITIONS: ModuleDefinitions;
/**
 * `entries` option for `rollup-plugin-alias`
 */
export declare const entries: {
    find: RegExp | string;
    replacement: string;
}[];
/**
 * `alias` option for `resolve` property of `webpack` configuration
 */
export declare const alias: Record<string, string>;
