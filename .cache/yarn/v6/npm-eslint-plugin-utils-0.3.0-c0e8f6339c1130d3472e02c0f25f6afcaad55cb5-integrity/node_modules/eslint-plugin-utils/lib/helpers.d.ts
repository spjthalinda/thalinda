import { Linter } from 'eslint';
export interface JsonMessage {
    severity?: Linter.Severity;
    message: string;
    ruleId: string;
}
export declare type JsonMessageHandler<T> = (jsonMessage: T, lintMessage: Linter.LintMessage) => JsonMessage;
export declare const registerJsonMessageHandler: <T>(ruleId: string, handler: JsonMessageHandler<T>) => void;
